import { CirclePointRenderTargetDirective } from './circle-point-render-target.directive';

describe('CirclePointRenderTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new CirclePointRenderTargetDirective();
    expect(directive).toBeTruthy();
  });
});
